UPDATE "PrinterLineItems"
SET "DeliveryDate" = CAST('{{moment().format('YYYY-MM-DD').toString()}}' AS DATE)
WHERE "Id" IN ({{`'${appsmith.store.updateAllOrderStatus.join("','")}'`}}) OR "SubItemID" IN ({{`'${appsmith.store.updateAllOrderStatus.join("','")}'`}});